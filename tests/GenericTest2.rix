import ("rsl/Vector.rix")

loadFactor->String(Vector v) = (v.length() + "/" + v.capacity())

#a = Vector{String}(100)
#i.for (0,10)
    a[i] = "i = " + i + " -> " + (i+2).sqrt()

i.for (4,9)
    print (a[i])

print ("a is " + loadFactor(a))

if (a.isNull(9))
    print ("No element at index 9")
else
    print ("Element exists at index 9")


#b = Vector{int}(100)
i.for (0,8)
    b[i] = i

i.for (4,9)
    print (b[i])

#bLoadFactor = loadFactor(b)

print ("b is " + bLoadFactor)

if (b.isNull(9))
    print ("No element at index 9")
else
    print ("Element exists at index 9")