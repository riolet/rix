bash make.sh        --compiles the program

Legend:
-----------------------------------------------------------------------------------------


Program files:

a.snazzle.file -- the file containing the sample program written in the snazzle language
snazzle.l      -- generated by the lexer (flex)
snazzle.y      -- generated by bison (.y for yacc)
snazzle        -- output file from gcc. Basically the toy compiler.


Temp Files:

snazzle.tab.c  -- output from bison
snazzle.tab.c  -- output from bison
lex.yy.c       -- output from flex and snazzle.l and snazzle.tab.c internally


Bash Files:

make.sh        -- usage <bash make.sh> compiles code
comp.sh        -- usage <bash comp>    compiles the flex and bison
clr.sh         -- usage <bash clr>     removes ALL temp files.
