BetterString :: String
    ::: String s
        print s.data

    plus->String: String right
        ->$+right+"Cake"
fib -> Integer:  Integer n
    if (n <= 1)
        ->n
    else
        ->(fib (n-1)) + (fib (n-2))

#N = Integer (args 1)

#i for 1, N+1
    print i + ": " + (fib i)


s = BetterString  "Hi"
print s
