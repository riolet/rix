//Does not work yet
/*
hanoi -> int: int n, ?s, ?a, ?e  =  (n == 1) tf (print s + " -> " + e), ((hanoi (n - 1), s, e, a) + (print s + " -> " + e)+(hanoi (n - 1), a, s, e))

hanoi (int (args 1)), "A", "B", "C"


a = Array int,5

genericType = ?s

::: ?gt, max
    genericType = ?gt

add -> Array: genericType g
    blah

*/

#a = (1<>2) leg 1, 2, "3"
print a
